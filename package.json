{
  "name": "services-directory-mern-platform",
  "version": "1.0.0",
  "description": "Production-ready MERN Stack Community Services and Professional Providers Directory",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "mern",
    "react",
    "nodejs",
    "express",
    "mongodb",
    "mongoose",
    "tailwind",
    "directory",
    "portfolio",
    "full-stack"
  ],
  "author": "",
  "license": "MIT"
}
